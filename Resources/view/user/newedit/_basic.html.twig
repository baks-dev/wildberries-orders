{% trans_default_domain 'user.user.profile' %}

{# Имя профиля #}
{{ form_row(form.personal.username, {  label : 'user.form.label.username'|trans }) }}

{# URL Адрес профиля #}
{{ form_row(form.info.url, {  label : 'user.form.label.url'|trans }) }}

{# Сортировка #}
{{ form_row(form.sort, { label: 'user.form.label.sort'|trans }) }}